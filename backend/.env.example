# ═══════════════════════════════════════════════════════════════════════════
# SENTRY - Environment Variables
# The Deterministic Vulnerability Prover
# ═══════════════════════════════════════════════════════════════════════════
# 
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to version control!
# ═══════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# AI PROVIDER CONFIGURATION
# ─────────────────────────────────────────────────────────────────────────────

# AI Provider to use: openrouter | openai | gemini
# Default: openrouter (free with rate limits)
AI_PROVIDER=openrouter

# ─────────────────────────────────────────────────────────────────────────────
# OPENROUTER CONFIGURATION (DEFAULT - FREE)
# ─────────────────────────────────────────────────────────────────────────────

# OpenRouter API key (optional for free models, required for premium)
# Get one at: https://openrouter.ai/keys
# Leave empty for free tier usage
OPENROUTER_API_KEY=

# OpenRouter model to use (supports comma-separated fallback chain)
# Best FREE models: moonshotai/kimi-k2, deepseek/deepseek-v3.2-speciale, z-ai/glm-4.5-air:free
# Format: primary,fallback1,fallback2 (tries next on failure)
OPENROUTER_MODEL=moonshotai/kimi-k2,deepseek/deepseek-v3.2-speciale

# ─────────────────────────────────────────────────────────────────────────────
# OPENAI CONFIGURATION
# ─────────────────────────────────────────────────────────────────────────────

# Your OpenAI API key (required if using OpenAI provider)
# Get one at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI model to use (default: gpt-4o)
OPENAI_MODEL=gpt-4o

# ─────────────────────────────────────────────────────────────────────────────
# GOOGLE GEMINI CONFIGURATION
# ─────────────────────────────────────────────────────────────────────────────

# Google Gemini API key (required if using Gemini provider)
# Get one at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Gemini model to use (default: gemini-1.5-flash)
# Options: gemini-1.5-flash, gemini-1.5-pro, gemini-pro
GEMINI_MODEL=gemini-1.5-flash

# ─────────────────────────────────────────────────────────────────────────────
# OPTIONAL CONFIGURATION
# ─────────────────────────────────────────────────────────────────────────────

# Analysis timeout in milliseconds (default: 30000)
# ANALYSIS_TIMEOUT_MS=30000

# Compiler timeout in milliseconds (default: 30000)
# COMPILER_TIMEOUT_MS=30000
